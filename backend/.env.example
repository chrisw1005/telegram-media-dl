# Get from https://my.telegram.org/apps (create a new application)
TG_API_ID=your_api_id_integer
TG_API_HASH=your_api_hash_hex

# Generate with:  python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
SESSION_ENCRYPTION_KEY=your_fernet_key_here

# Phase 2 (Bot) — leave empty for P1
BOT_TOKEN=
WEBHOOK_SECRET=
